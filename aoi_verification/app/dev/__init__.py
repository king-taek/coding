"""개발자 모드 — 매칭 단계 가속 조합(레시피) 실험·기록 하니스.

이 패키지는 **개발자/엔지니어 전용**이다.  매칭(Stage 2)의 '연산을 어느 장치
(CPU·Intel GPU·Intel NPU)에서 어떻게 수행할지' 조합을 정의(``recipes``)하고,
각 조합을 **유사도 캐시를 우회한 채**(처음 매칭처럼) 실행해 속도·메모리·**실제
정확도**를 측정·기록한다(``benchmark``).

설계 원칙
- 일반 사용자 흐름에는 영향을 주지 않는다(기본 비활성, 환경변수/명시적 진입).
- 정확도(실제 매칭이 맞았는지)는 절대 양보하지 않는다 — 추천은 '정확도를
  보존하면서 가장 빠른' 조합만 고른다.
- 시스템이 로딩으로 꺼지지 않도록 메모리 예산·동시성 상한·타임아웃·서브샘플을
  둔다(레시피는 항상 순차 실행).
"""

from __future__ import annotations
