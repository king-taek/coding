# 디바이스 하드코딩 조사 — TB500 기준값이 박혀 있던 곳과 대체 소스

> 배경: 평소와 다른 device 로 검증을 돌렸더니 **좌표가 완전히 엉망**으로 나왔다.
> 원인을 추적해 "TB500 한 대의 실측값이 상수로 박혀 있던 곳"을 전수 조사하고,
> 그 값들을 **스캔 결과 폴더의 파일에서 읽도록** 바꾼 기록이다.
>
> 결론부터: **필요한 값은 이미 결과 폴더 안에 다 있었다. 코드가 안 읽고 있었을 뿐이다.**

---

## 1. 무엇이 하드코딩돼 있었나

`aoi_verification/app/coords/models.py` 에 die 격자 상수 6개가 있었고, 좌표 변환 전체가
이 값에 의존했다. die 크기가 다른 디바이스에서는 `x = X − Col × 37247.7` 이 통째로
틀린 값을 낸다 — 이것이 "좌표가 엉망" 의 직접 원인이다.

| 상수 | TB500 값 | 쓰이는 곳 | 대체 소스 | 상태 |
|---|---|---|---|---|
| `CAMTEK_PITCH_X` | 37247.7 µm | `camtek_ini.py` `x = X − Col×pitch` | `Params_WaferInfo.ini` `[Geometry] DieStep_X` | ✅ 전환 완료 |
| `CAMTEK_PITCH_Y` | 44905.4 µm | `camtek_ini.py` `y = Y − Row×pitch` | `[Geometry] DieStep_Y` | ✅ 전환 완료 |
| `KLA_ZERO_X` | 3 | `kla_info.py` `col = XINDEX + 3` | `.001` `SampleTestPlan` 의 `min(XINDEX)` | ✅ 전환 완료 |
| `KLA_ZERO_Y` | 3 | `kla_info.py` `row = YINDEX + 3` | `SampleTestPlan` 의 `min(YINDEX)` | ✅ 전환 완료 |
| `CAMTEK_COL_OFFSET` | 2 | `camtek_ini.py` `col = Col − 2` | ⚠ 아직 어떤 파일에도 없음 | §5 |
| `CAMTEK_ROW_TOTAL` | 6 | `camtek_ini.py` `row = 6 − Row` | ⚠ 아직 어떤 파일에도 없음 | §5 |

> `DieSize_Y`(44904.9858) 가 아니라 **`DieStep_Y`(44905.4)** 가 맞다 — 옛 상수와 정확히 일치한다.
> `DieSize` 는 `DieStep` 이 없을 때의 폴백으로만 쓴다.

### 그 밖의 device 관련 상수

| 위치 | 값 | 판단 |
|---|---|---|
| `coord_matcher.py` `CONFIDENT_DIST = 20.0` µm | '거의 정확히 일치' 반경 | ✅ **tol 의 절반으로 묶음**. die 가 작은 자재는 tol 을 20 µm 근처까지 낮춰 쓰는데, 그러면 모든 매치가 '확정' 이 돼 차순위 후보가 사라졌다. tol=500(기본)에서는 20 그대로라 기존 동작 불변 |
| `i18n/ko.py` 허용 오차 툴팁 | "TB500 한 die 크기 대비 약 1%" | ✅ device 이름 제거 |
| `models.py` `SURFACE_LEN_FACTOR = 0.77` µm/px | 계측정보 px→µm 폴백 | 이미 `coords/pixel_size.py` 가 `Params_WaferInfo.ini:RefPixelSizeX` 등 4곳을 우선 읽는다 — **양호** |
| `models.py` `SURFACE_AREA_FACTOR = 0.5929` | — | **현재 아무도 안 쓴다**(기존 dead code). 이번 변경 범위 밖이라 그대로 뒀다 |
| `coords/surface_flt.py` 152바이트 레코드 배치 | TB500 84건 brute-force | 계측정보(area/blob/contrast) 전용이라 좌표와 무관. **다른 디바이스에서 계측정보가 이상하면 여기부터 의심할 것** |
| `camtek_live.py` `row − 1` | LIVE 파일명 규약 | 실측상 device 무관 — LIVE 파일명은 이미 표준 col/row 를 담고 있다. 유지 |

---

## 2. 실측으로 확인한 불변식 — pitch 검산의 근거

`dev/좌표 확인/Camtek/예시1~3` 의 `ColorImageGrabingInfo.ini` **전 항목 63건**에서:

```
Col == floor(X / DieStep_X)      63 / 63 일치
Row == floor(Y / DieStep_Y)      63 / 63 일치
```

→ **Camtek 의 die 격자는 stage 원점 (0,0) 에 고정**돼 있고, `Params_WaferInfo.ini` 의
`DieStep` 이 바로 그 격자 간격이다.

이 관계는 그대로 **자체 검산**에 쓴다. 파일에서 pitch 를 읽은 뒤 같은 폴더 INI 의 Col/Row 로
위 식을 검사해서, 안 맞으면 그 값을 **채택하지 않고** 폴백 + 경고를 남긴다. 엉뚱한 키를
읽었거나 다른 웨이퍼의 INI 가 섞였을 때 조용히 틀린 좌표를 내지 않게 하는 장치다.

---

## 3. "KLA 에도 이런 정보가 있나요?" → **있습니다. 오히려 더 많습니다.**

`dev/좌표 확인/KLA/예시1/*.001` (KLARF 형식) 헤더 실물:

```
SampleSize 1 300;                                    ← 300 mm 웨이퍼
DeviceID "4DT-TB500-H-M1";                           ← device 이름이 데이터에 그대로 있다
StepID "RDL2_NEW(MULTI)";
SampleOrientationMarkType NOTCH;
OrientationMarkLocation DOWN;                        ← notch 방향(축 뒤집기 근거)
DiePitch 3.7247930000e+004 4.4905340000e+004;        ← die pitch X, Y (µm)
DieOrigin 0.0000000000e+000 0.0000000000e+000;
WaferID "W6459076XYG1";                              ← slot 명 (이미 읽고 있음)
Slot 10;
SampleCenterLocation 1.8460000000e+004 -2.2294100000e+002;
SampleTestPlan 30
  -3 -1   -3 0   -2 -2  …  3 -1   3 0 ;              ← 검사한 die 인덱스 전체 목록
```

`SampleTestPlan` 이 곧 `KLA_ZERO_X=3` 의 근거였던 "package X count 7 → 7//2 = 3" **그 자체**다.
실측 범위가 XINDEX −3..3(7열), YINDEX −3..2(6행) 이므로 `zero = −min(index)` 로 **정확히 3, 3**
이 나온다. 즉 **KLA 쪽은 100% 데이터 기반 전환이 가능했고, 실제로 전환했다.**

| 항목 | 전환 전 | 전환 후 |
|---|---|---|
| `DiePitch` | **Y 하나만** 읽음(y 뒤집기용) | X·Y 모두 읽음 |
| `SampleTestPlan` | 안 읽음 | **die 인덱스 원점 산출** |
| `DeviceID`/`LotID`/`StepID`/`Slot` | 안 읽음 | (여전히 안 읽음 — 필요해지면 여기서) |
| `DieOrigin`/`SampleCenterLocation` | 안 읽음 | (절대좌표 환산이 필요해지면 여기서) |
| `OrientationMarkLocation` | 안 읽음 | (축 뒤집기 방향을 데이터로 정할 때) |

---

## 4. Camtek `WaferInfo.ini` — 코드가 전혀 안 읽는 파일

```
[AutoCycleInfo]
InputLot=TBD-PIDS3
InputWaferID=25195007EWF6      ← KLA 의 WaferID "…" 와 정확히 대응
Machine=EagleG5_109076
ActiveSlot=5
CarrierID=12612202
```

KLA 는 `.001` 헤더의 `WaferID` 로 폴더명과 무관하게 slot 을 식별하는데(`kla_info.read_wafer_id`),
**Camtek 은 폴더명에만 의존**한다. `InputWaferID` 를 읽으면 Camtek 쪽도 같은 수준의 식별이
가능하다.

→ 이번 변경 범위(하드코딩 제거)와는 별개 기능이라 **기록만 하고 손대지 않았다.**
필요하면 `kla_info.read_wafer_id` 와 같은 모양으로 추가하면 된다.

---

## 5. 남은 하나 — Camtek `col_origin` / `row_total`

`CAMTEK_COL_OFFSET=2`, `CAMTEK_ROW_TOTAL=6` 은 **die 격자의 실제 범위**에서 나온 값이다
(실측: Col 2..8 = 7열, Row 1..6 = 6행 — KLA 의 XINDEX −3..3, YINDEX −3..2 와 정확히 대응).
그런데 이 범위는 `Params_WaferInfo.ini` 에도 `WaferInfo.ini` 에도 없다.

시도해 본 대안과 결과:

| 방법 | 결과 |
|---|---|
| `Diameter / DieStep` 로 기하 계산 | ✗ 격자가 300 mm 를 다 안 채운다(실측 폭 261 mm) |
| 웨이퍼 1장의 INI 에서 `min(Col)`/`max(Row)` | ✗ 웨이퍼마다 다르다 (예시1 은 Col 2..8, 예시2·3 은 3..8) |
| 결함이 있는 die 만 INI 에 나옴 | ✗ 격자 가장자리 die 에 결함이 없으면 범위를 놓친다 |

**→ Camtek 웨이퍼 결과 폴더의 전체 파일 목록을 확인해 die map 을 담은 파일을 찾아야 한다.**
`wafer_geometry._CAMTEK_SOURCES` 튜플에 한 줄 추가하면 되도록 구조를 만들어 뒀다.

### 그때까지의 동작 — 왜 급하지 않은가

두 값은 **표시·엑셀의 col/row 숫자에 일정 오프셋**으로만 남는다. 매칭에는 영향이 없다:
`ref` 와 `val` 양쪽에 똑같은 오프셋이 걸리므로 `(col,row) ±1` 이웃 게이트와 거리 계산에서
**상쇄**된다. 실제로 문제가 됐던 **Camtek↔Camtek 매칭 실패는 pitch 수정만으로 해소**된다.

---

## 6. 어떻게 바꿨나

새 모듈 **`aoi_verification/app/coords/wafer_geometry.py`** 가 폴더별 die 격자 기하를 제공한다.
`coords/pixel_size.py` 가 이미 "상수처럼 보이던 0.77 을 폴더에서 읽어 대체" 한 선례가 있어
그 관습을 그대로 따랐다 — 폴더 단위 `@lru_cache`, 우선순위 소스 튜플, 전 구간 fail-safe.

```
camtek_geometry(folder) → CamtekGeometry(pitch_x, pitch_y, col_origin, row_total, source)
kla_geometry(folder)    → KlaGeometry(pitch_x, pitch_y, zero_x, zero_y, source)
```

- **Camtek**: 폴더 → 부모 2단계까지 `Params_WaferInfo.ini` 를 찾는다
  (실제 배치가 `…\<Lot>\<WaferID>\Params_WaferInfo.ini` 라 사진이 하위에 있을 수 있다).
  `DieStep_X/Y` → 없으면 `DieSize_X/Y`. 범위 클램프 후 §2 의 식으로 검산.
- **KLA**: `.001` 헤더 64 KB 만 읽어 `DiePitch` 와 `SampleTestPlan` 파싱.
- **폴백**: `models.py` 의 TB500 상수. 폴백을 탈 때는 **반드시 경고 로그를 남긴다**
  (조용히 옛 경로로 떨어지면 아무도 모른다 — `AsyncInferQueue` 때 겪은 그 사고).

`models.py` 의 상수 6개는 이름·값 그대로 남아 있지만, 이제 **폴백 전용**이다.

---

## 7. 회귀 가드

`dev/tests/test_wafer_geometry.py` (무거운 의존성 없이 헤드리스):

- `Params_WaferInfo.ini` → pitch 파싱 (`DieStep` 우선, `DieSize` 폴백, 범위 밖 거부, 부모 탐색)
- 읽은 pitch 가 `Col == floor(X/pitch)` 와 안 맞으면 **채택하지 않는다**
- KLA `SampleTestPlan` → die 인덱스 원점 (실측 헤더에서 **3, 3** — 옛 상수와 일치)
- **가상의 non-TB500 디바이스**(pitch 10000×12000, 격자 원점 1/4)로 end-to-end 변환
- 실측 폴더가 있으면 §2 불변식 63건 + 보고서 기준 좌표 `(5,3)` / `x 11913.861` 재확인

`dev/tests/test_coord_matcher.py` 에 `CONFIDENT_DIST` 의 tol 연동 가드를 추가했다.
`dev/tests/test_coords.py` 는 TB500 격자를 **명시적으로** 넘기도록 바꿔, 기대값이 어느
디바이스 기준인지 드러나게 했다(값·기대 결과는 그대로).
