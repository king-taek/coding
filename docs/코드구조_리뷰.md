# 코드 구조 리뷰 — 죽은 코드 · 구조 개선안

작성 시점 기준 브랜치: `claude/ui-improvements-review-jt6xcj`

이 문서는 **보고서**다.  아래 제안 중 어떤 것도 아직 적용하지 않았다.  각 항목마다
`개선안 / 단점 / 리스크` 를 함께 적었으니, 할 것과 하지 않을 것을 골라 지시하면 된다.

---

## 0. 요약

| 지표 | 값 |
|---|---|
| 앱 코드 | 26,725줄 / 99파일 |
| 테스트 | 15,641줄 / 84파일 |
| 테스트 커버리지 | 74% |
| 미사용 i18n 문자열 | **1** / 344 |
| 미사용 테마 토큰 | **0** |
| 미사용 QSS role | **0** / 37 |
| 미사용 import | **0** (`from __future__` 제외) |
| 100줄 초과 함수 | 14 / 1,118 |
| 패키지 순환 의존 | 1건 (지연 import 로 회피 중) |

**결론부터**: 이 저장소는 흔히 기대하는 종류의 쓰레기 — 미사용 import, 죽은 상수,
안 쓰는 문자열, 고아 위젯 — 가 **거의 없다**.  i18n 344개 중 미사용 1개, 테마 토큰
미사용 0개는 이례적으로 잘 관리된 수치다.  그래서 "여기저기 조금씩 지우기" 로는
얻을 게 없다.

대신 **한 덩어리의 큰 죽은 기능**과 **두 개의 구조적 부채**가 있다.

1. **CNN 임베딩 기능이 통째로 죽어 있다** — 약 390줄.  가장 확실하고 값이 크다.
2. `MainWindow` 가 9가지 역할을 겸한다 — 1,800줄.
3. UI 생성 코드가 100~200줄짜리 일직선 스크립트다.

> **[이미 조치함] 리뷰 중 진짜 크래시를 하나 찾아 고쳤다.**
> `embedder_openvino._preprocess_parallel` 이 이미 지워진 인자 `px` 를 넘기고 있어
> `NameError` 로 죽고 있었다.  **Intel GPU 가 있는 사용자 PC 에서만** 도달하는
> 경로(개발 PC·CI 는 GPU 가 없어 안 탄다)이고 `# pragma: no cover` 라 커버리지에도
> 안 잡혀, 고효율 모드가 첫 사진에서 죽는 상태였다.  호출부를 고치고 GPU 없이도
> 도는 회귀 테스트(`dev/tests/test_preprocess_parallel.py`)를 붙였다.
> — 죽은 코드를 뒤지는 일의 실제 값은 대개 이런 데서 나온다.

---

## 1. 죽은 코드 — CNN 임베딩 기능 (약 390줄)

### 무엇이 죽었나

`similarity/cnn_embed.py` 는 **리터럴로** 비활성이다:

```python
def is_available() -> bool:
    return False                     # ← 상수

def compute_embedding(src) -> Optional[np.ndarray]:
    return None                      # ← 상수
```

이걸 켜려면 학습된 `.pt` 가중치와 `learning/registry.py` 의 active 포인터가 필요한데,

- **`.pt` 를 만드는 학습 기능은 이미 제거됐다** (`learning/__init__.py` 가 명시).
- **`learning/registry.py` 를 import 하는 앱 코드가 0곳이다** (테스트만 씀).
- `config.SimilarityWeights.use_cnn` 기본값이 `False` 이고, 이를 `True` 로 바꾸는
  코드도 없다.

즉 되살릴 경로 자체가 없다.

### 연쇄 — 파이프라인 실행 경로도 함께 죽었다

이게 이번 리뷰에서 가장 중요한 발견이다:

```python
# similarity/slot_features.py:434
def _should_pipeline(self) -> bool:
    ...
    return bool(_cnn.is_available())      # ← 항상 False
```

`run()` 이 이 값으로 `_run_pipelined()` 와 `_run_sequential()` 을 고르므로,
**`_run_pipelined()`(52줄)는 프로덕션에서 단 한 번도 실행되지 않는다.**
producer/consumer 파이프라이닝 전체가 도달 불가 코드다.

주석에도 그렇게 적혀 있다 — "겹칠 GPU 작업은 CNN 임베딩뿐인데, 그 경로가 비활성이라
지금은 항상 False 다".  코드는 남겨 두고 사실만 주석으로 기록한 상태다.

### 삭제 후보 목록

| 줄 수 | 대상 | 근거 |
|---|---|---|
| 60 | `learning/registry.py` **전체** | 앱 참조 0회. `.pt` 산출 수단 없음 |
| 39 | `similarity/cnn_embed.py` **전체** | 리터럴 `False`/`None` |
| 77 | `slot_features` 의 `_run_pipelined`·`_should_pipeline`·`_prefetch_cnn_embeddings` | 도달 불가 |
| ~34 | `similarity/pipeline.py` 의 cnn 분기 + `Feature.cnn`·`cnn_model` 필드 | |
| ~15 | `config.py` 의 cnn 가중치 + `normalized()` 의 재분배 분기 | `use_cnn` 이 항상 False |
| 5 | `utils/paths.models_dir()` | registry 전용 |
| 37 | `dev/tests/test_registry.py` | 죽은 모듈의 테스트 |
| 120 | `dev/tests/test_pipeline_equivalence.py` | 도달 불가 경로만 검증 |
| **약 390** | **합계** (앱 232 + 테스트 157) | 앱 코드의 0.9% |

### 리스크: **낮음**

- **옛 점수 캐시 호환**: `Feature.load` 가 이미 `if "cnn" in data.files` 로 방어하고
  있어, 필드를 지워도 기존 `.npy` 를 그대로 읽는다.  반대로 옛 캐시에 남은 cnn 키는
  무시된다.  양방향 호환.
- **고효율 모드와 무관**: GPU 임베딩(고효율 모드)은 `learning/embedder_openvino.py`
  가 동봉 IR 로 따로 처리한다.  이번 삭제 대상과 코드가 겹치지 않는다 — 확인함.
- **`_score_pairs_parallel`(93줄)은 살아 있다** — 순차 경로도 쓴다.  삭제 대상 아님.

### 단점

- 나중에 CNN 재채점을 되살리고 싶으면 git 히스토리에서 꺼내야 한다.  다만 되살리려면
  어차피 학습 파이프라인부터 다시 만들어야 하므로, 지금 코드가 그때 쓸 만할 가능성은
  낮다.
- 커버리지 숫자가 살짝 흔들린다(`registry.py` 는 지금 100% 다 — 자기 테스트만으로).
  이건 오히려 **커버리지가 죽은 코드를 살아있는 것처럼 보이게 한** 사례다.

---

## 2. 그 밖의 확실한 죽은 코드 (합계 ~180줄)

### 2-1. 통째로 죽은 모듈 2개

| 줄 | 대상 | 근거 | 리스크 |
|---|---|---|---|
| 48 | `utils/errors.py` **전체** | 프로덕션 import **0곳**.  `log_silent` 를 부르는 코드가 없다 — 모듈 docstring 이 권하는 채택 패턴이 끝내 채택되지 않았다(조용한 `except` 100여 곳 중 단 한 곳도 안 씀).  `errors.logger` 도 미사용(다들 `logging.getLogger` 직접 호출) | 낮음 (+테스트 7줄) |
| 46 | `workers/thumbnailer.ThumbnailWorker` | 생성자 호출 **0곳**.  docstring 이 "호환용(기존 호출자 유지)" 이라는데 그 호출자가 이미 없다 — 지금은 `ThumbnailPool` 을 쓴다.  연쇄로 `main_window.py:1778-1780` 의 종료 처리도 도달 불가(`_thumb_worker` 에 값이 대입되는 곳이 없다) | 낮음 |

### 2-2. 절대 안 불리는 것 · 쓰기만 하고 안 읽는 것

| 대상 | 근거 | 리스크 |
|---|---|---|
| `sheet_host.about()` | 호출 0곳 (`run 18 · info 15 · warn 10 · ask 7 · choose 2 · error 1 · about 0`) | 낮음 |
| `models/session.py` 의 `decision_history`·`phase_a_decisions`·`phase_a_no_match`·`cross_matches_b`·`cross_skipped_b` | 교차검증 제거의 잔해.  생성도 읽기도 안 함.  `from_dict` 가 미지 키를 걸러 옛 세션 JSON 호환 | 낮음 |
| `models/result.py` 의 `FinalResult.mode`·`miss_fast`·`miss_slow` | 쓰기만 하고 읽는 곳 0 (`miss_fast`/`miss_slow` 는 항상 `[]` 대입) | 낮음 |
| `main_window.py:20` `Qt` import | 프로덕션 전체에서 **유일한** 미사용 import | 없음 |
| `setup_page.apply_state(mode=…)` | 본문이 `mode` 를 안 씀 | 없음 |
| 고아 상수 8개 (`IMAGE_INFO_COL_ITEM`·`config.top_k`·`has_preprocess`·`_CAND_RATIO`·`_TILE_W`·`_STRIP_REL_TARGETS`·`theme.ACCENT_HOVER/ACCENT_PRESSED` 전역·`TOKENS["pass_tint"]`) | 각각 참조 1회(정의 자신) | 없음 |

### 2-3. 문서가 없는 파일을 가리킨다 (6건, 문서만 수정)

`docs/화면_디자인_도면.md` → `setup_layouts.py`·`test_setup_layouts.py`·`capture_setup.py`·`capture_all.py`
`docs/외부도구_탐지_보고서.md` → `create_conda.py`·`build_windows.bat`

**지금 고치는 것을 권한다** — 없는 파일을 가리키는 문서는 다음 사람을 헛수고시킨다.

### 2-4. 판단이 필요한 것 (지우기 전에 의도 확인)

| 대상 | 상황 | 왜 판단이 필요한가 |
|---|---|---|
| `config.pick_tier` 의 자동 티어 선택 | `main_window.py:942` 가 `speed_mode=True` 를 **하드코딩**해 항상 마지막 티어를 반환 → 4행 티어 표 중 3행이 도달 불가.  `per_side_total` 을 계산해 버리고, "화질이 낮아졌다" 안내가 **항상** 뜬다 | 의도적 성능 결정(주석에 "상시 적용")이지만 죽은 표와 항상 뜨는 안내는 오해를 부른다.  티어를 지우든 `speed_mode` pref 를 되살리든 하나를 골라야 |
| `prefs` 의 `last_ref_root`·`last_val_root`·`last_ref_machine`·`last_val_machine`·`last_mode` | **저장은 하는데 아무도 읽지 않는다** | '마지막 폴더 기억' 이 원래 원했던 기능일 수 있다.  지우면 그 회귀를 덮는 셈 |
| `prefs` 의 `image_long_edge_select`·`image_long_edge_match`·`splitter_state_select_v`·`speed_mode` | 읽지도 쓰지도 않음 | 안전 |
| `SimilarityConfig.center_crop`·`orb_nfeatures` | 프로덕션에서 항상 기본값.  관련 crop/ORB 경로 전부 도달 불가 | **높음** — 캐시 키 입력이다.  지우면 사용자 디스크의 특징·점수 캐시가 통째로 무효화된다.  캐시 버전을 올릴 각오가 있을 때만 |
| `main_window._autosave` 가 만드는 상태의 대부분 | 이어하기(`_maybe_resume`)는 7개 필드만 읽는데, `decisions`·`matches`·`skipped`·`no_match` 등 ~40줄을 매 결정마다 직렬화 | **높음** — 세션 중간 재개가 예정된 기능일 수 있다 |
| 테스트만 쓰는 공개 API 8종 (`insecure_fallback_used`·`COLOR_MODE_LABELS`·`PRIORITY_CENTER`/`PRIORITY_NEXT_SLOT`·`lru_pixmap_cache` 접근자 3개 등) | 프로덕션 호출 0 | 일부는 정당한 테스트 심(seam).  `insecure_fallback_used`·`COLOR_MODE_LABELS` 는 버려진 UI 훅으로 보임 |

---

## 3. 삭제하면 안 되는 것 (오판 방지)

리뷰 중 "죽은 것처럼 보였지만 살아 있는" 것들이다.  실수로 지우지 않도록 기록한다.

| 대상 | 왜 살아 있나 |
|---|---|
| `workers/wafer_id_ocr.py` (커버리지 **0%**) | rapidocr 가 설치돼 있고 정보파일이 없을 때만 도는 폴백.  **미테스트일 뿐 죽지 않았다** |
| `widgets/matches_review.py` (커버리지 20%) | 결과 화면 '매칭 결과 검토' 버튼의 실제 구현.  `match_review_page` 와 진입점이 다르다 |
| `scripts/internal/make_exe_guide_pdf.py` (참조 0, 430줄) | 커밋된 `docs/exe_만들기_가이드.pdf` 의 **생성기**.  지우면 PDF 를 다시 만들 수 없다 |
| UI 의 함수 내 `import` 10곳 | 시작 속도를 위한 **의도적 설계**.  `test_startup_light_import.py` 가 지킨다.  최상위로 올리면 시작이 느려지고 그 테스트가 깨진다 |
| `updater.download_and_apply` (104줄) | CLAUDE.md 가 로딩바 계약의 **참조 구현**으로 지목한 함수 |
| 테스트 전용 심볼 4개 (`theme.color_mode_keys`·`pixel_size.scan_pixel_size`·`embedder_openvino.target_device`·`updater.insecure_fallback_used`) | 정당한 테스트 심(seam).  지우면 테스트가 내부 구현을 직접 찌르게 된다 |

---

## 4. 구조 개선안 — 값/리스크 순

### 4-1. 지금 해도 되는 것 (리스크 낮음)

| # | 개선안 | 값 | 단점 · 리스크 |
|---|---|---|---|
| 1 | **빠른 테스트 레인 오염 제거.** `-m "not ui"` 가 빠른 레인인데, Qt 를 쓰는 테스트 4개가 픽스처를 안 받아 `ui` 마커를 못 받고 섞여 있다 (`test_color_mode.py` 2건, `test_precompute_progress.py`, `test_variants.py`).  함수 인자에 `qapp` 한 단어 추가로 해결 | 높음 | 단점 없음.  리스크 **없음** |
| 2 | **조용한 예외 3곳을 로그로.** `main_window.py` 의 자동저장(1766)·실행로그(1383)·타일캐시 초기화(849) 실패가 `except: pass` 로 사라진다.  특히 자동저장 실패는 '이어하기' 가 조용히 죽는 것 | 높음 | 로깅 import 3개 추가.  제어 흐름 불변이라 리스크 **낮음** |
| 3 | **레이어 회귀 테스트 추가.** 지금 `workers`·`models`·`utils` 등 하위 패키지가 `ui` 를 import 하는 곳이 **0곳**이다(좋은 상태).  15줄짜리 테스트로 고정 | 높음 | 테스트만 추가.  리스크 **없음** |
| 4 | **`MatchPage.review_context()` 도입.** `MainWindow` 가 `match_page` 의 비공개 속성을 `getattr` 로 8곳에서 찔러 보고, 그중 8줄짜리 엔진 설정 추출 블록이 **두 번 복붙**돼 있다(1270·1332) | 높음 | 데이터클래스 1개 추가.  리스크 **낮음** (지금도 전부 기본값 방어 중) |
| 5 | **`qapp` 픽스처 31개 중복 제거.** 31개 테스트 모듈이 conftest 와 **바이트 동일한** 픽스처를 재정의한다 | 중간 | 31파일 diff — '수술적 변경' 원칙과 상충.  런타임 이득 0(가독성만).  리스크 **낮음** |
| 6 | **격자 재배치 공식 통일.** 올바른 공식(off-by-one 수정 포함)이 `option_group.reflow_into_grid` 에 이미 있는데, `bulk_select_dialog`·`thumb_grid` 는 각자 틀린 공식을 쓴다 | 중간 | 열이 하나 늘어 보일 수 있음 → '가로 스크롤 금지' 규칙과 충돌 가능.  리스크 **중** |
| 7 | **`models ↔ utils` 순환 해소.** `utils/wafer_id.merge_unmatched_by_wafer_id` 를 `models/slot.py` 로 옮기면 끝.  지금은 함수 내 지연 import 로 회피 중이라 런타임 문제는 없다 | 중간 | 리스크 **낮음** |
| 8 | 상태바 스타일 문자열 중복(`main_window.py:122`·`1671`) 을 상수로 | 낮음 | 리스크 **없음** |

### 4-2. 값은 크지만 신중해야 하는 것 (리스크 중)

| # | 개선안 | 단점 · 리스크 |
|---|---|---|
| 9 | **KLA 해석 로직 분리** (`main_window.py:603–803`, 231줄 → `ui/flows/kla_resolve.py`).  이미 콜백 형태라 이음매가 깨끗하다 | **CLAUDE.md 가 `main_window._ask_kla_side`·`_kla_resolve_impl` 을 이름으로 지목**해 4경우 계약(ref/val/both/None)의 위치로 못 박아 뒀다.  옮기면 같은 커밋에서 CLAUDE.md 도 고쳐야 한다.  실패 모드: `{**info_ref, **ocr_ref}` 병합을 빠뜨리면 같은 WaferID 인데 병합 실패 → 슬롯이 결과에서 사라진다 |
| 10 | **`MatchPage` 의 사전계산 워커 오케스트레이션 분리** (~370줄 → `_precompute_driver.py`) | 실패 모드: 죽은 워커의 늦은 `slot_finished` 재진입.  지금은 `_precompute_signal_is_current` 가드가 막고 있는데, 이걸 원자적으로 함께 옮기지 않으면 "Stage 2 가 첫 장만 보여주고 멈춤" 이 재발한다 |
| 11 | **`select_page._build`(197줄)·`match_page._build`(194줄) 분해** | 두 페이지가 구조상 쌍둥이라 공유 믹스인이 유혹적이지만, `SelectPage` 만 3분할 splitter + 콜드스타트 로직이 있어 순진하게 합치면 Stage 1 콜드스타트가 깨진다 |

### 4-3. 하지 말 것

| 대상 | 이유 |
|---|---|
| **자동 업데이트 흐름 분리** (`main_window.py:257–381`) | CLAUDE.md 가 `updater.download_and_apply(progress=)` → `_update_progress`(시그널) → `_on_update_progress` → `LoadingOverlay.set_progress` 사슬을 **로딩바 계약의 참조 구현**으로 지정했다.  옮기면 그 참조가 무효가 되고, 이 UI 배선은 테스트가 없어 회귀가 **조용히** 난다 |
| **다크 모드 재생성 분리** (`main_window.py:1563–1666`) | "잠금은 어떤 경로로도 반드시 풀린다" 는 불변식이 걸려 있다.  깨지면 다크 모드를 **영구히** 못 바꾼다 — 원래 버그보다 나쁘다 |
| **UI 의 함수 내 import 를 최상위로 정리** | 시작 속도를 위한 의도적 설계.  `test_startup_light_import.py` 가 깨진다 |

---

## 5. 중복 코드 — 한 헬퍼로 묶을 것

지우는 것과 별개로, **같은 코드가 여러 벌 있는 곳**이다.

| 중복 | 위치 | 절감 |
|---|---|---|
| `_fmt_score(score, coord_mode, tolerance)` — **바이트 동일** | `matches_review.py:31` · `unmatched_review_dialog.py:46` (+ `match_review_page.py` 에 풀어 쓴 변형 3곳) | ~30줄 |
| 지연 썸네일(`_image_loaded` → `paintEvent` → `singleShot(0, load)` → `_source_pix` → `_apply_scaled` → `set_size`) — **3벌** | `match_review_page._LazyThumb` · `unmatched_review_dialog._CandidateTile` · `matches_review._Row` (+ `zoom_window._MidTile` 은 즉시 로드 변형) | ~80줄 |
| 원본 디코드 + 폴백(`QPixmap` → `isNull()` → `fill(PANEL)`) | `side_by_side_viewer._decode_original` · `unmatched_review_dialog._load_full_pixmap_scaled` | ~15줄 |
| `QScrollArea` 인라인 스타일 문자열 — 3벌 동일 | `match_page.py:217` · `select_page.py:394` · `match_expand_view.py:117` | QSS 로 |
| `f"color: {theme.X}; font-weight: 700;"` — 7곳 | 여러 위젯 | 기존 `role="cardTitle"` 로 |
| 상태바 크레딧 스타일 문자열 2벌 | `main_window.py:122` · `1671` | 상수로 |
| 격자 열 계산 공식 — 올바른 것은 `option_group.reflow_into_grid` 하나뿐 | `bulk_select_dialog` · `thumb_grid` 가 각자 틀린 공식 사용 | 4-1 #6 참조 |

**주의**: 지연 썸네일 통합은 값이 가장 크지만(~80줄) **성능이 민감한 경로**다.
`match_review_page.py:106` 에 "인스턴스별 setStyleSheet 를 쓰지 마라(#렉)" 는
회귀 방지 주석이 있으니, 어떤 헬퍼를 만들든 role 프로퍼티 방식을 유지해야 한다.

`fit_scale` 이 `zoom_window` 와 `side_by_side_viewer` 에 두 벌 있는 것은
**의도적 중복**이다(docstring 에 이유 명시 — `image_io`/numpy 를 끌어오지 않으려고).
건드리지 말 것.

---

## 6. 추천 실행 순서

가장 값이 크고 리스크가 낮은 것부터:

1. ~~**`px` NameError 수정**~~ — **이미 완료**(커밋 `92b2d7a`).  유일한 실사용 크래시였다.
2. **통째로 죽은 모듈 2개 제거** (2-1: `utils/errors.py`, `ThumbnailWorker`) — ~100줄, 리스크 낮음
3. **CNN 죽은 기능 제거** (1장) — ~390줄, 단일 커밋, 전체 테스트로 검증
4. **자잘한 죽은 것 + 문서 참조 6건** (2-2, 2-3) — ~80줄
5. **빠른 레인 오염 제거 + 레이어 테스트 추가** (4-1 #1, #3)
6. **조용한 예외 3곳 로그로** (4-1 #2)
7. **`_fmt_score` 중복 제거** (5장 첫 줄) — 바이트 동일이라 무위험
8. **`review_context()` 도입** (4-1 #4)

여기까지가 "확실히 이득, 거의 무위험" 구간 — 합계 **약 600줄**을 지우거나 합친다.

9번 이후(지연 썸네일 통합·격자 공식 통일·KLA 분리·사전계산 드라이버 분리)와
2-4장의 '판단 필요' 항목들은 각각 따로 결정하는 것을 권한다.  특히
`center_crop`/`orb_nfeatures` 는 **사용자 캐시를 무효화**하므로 별도 판단이 필요하다.

지시해 주시면 2번부터 순서대로 진행하겠다.
